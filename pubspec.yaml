name: adaptive_dialog
description: A new Flutter package project.
version: 0.2.3
homepage: https://www.app-logik.de/
repository: https://github.com/applogik/adaptive_dialog

environment:
  sdk: '>=2.18.1'
  flutter: '>=2.12.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
